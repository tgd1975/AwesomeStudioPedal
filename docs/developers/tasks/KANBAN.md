# Kanban Board

_Auto-generated by `housekeep.py`. Do not edit manually._

**Epics:** [distribution](#distribution) · [feature_test](#feature_test) · [iphone-app](#iphone-app) · [video-content](#video-content) · [Other](#other)

## distribution

_⚪ 2 open · 🔵 0 active · 🟡 0 paused · 🟢 0 closed · ░░░░░░░░░░ 0%_

```mermaid
kanban
  Open
    TASK_160["Publish app to Google Play Store"]
    TASK_179["Determine how to add the Android app to the release on GitHub"]
  Active
  Paused
  Closed
```

## feature_test

_⚪ 1 open · 🔵 0 active · 🟡 2 paused · 🟢 42 closed · █████████░ 93%_

```mermaid
kanban
  Open
    TASK_248["BLE pairing test — Windows manual fallback (and macOS if a host appears)"]
  Active
  Paused
    TASK_226["Feature Test — CLI scan with two pedals (S-04)"]
    TASK_249["nRF52840 pairing_pin is entirely unwired (security parity with ESP32)"]
  Closed
    TASK_149["Feature Test — CLI validate command"]
    TASK_150["Feature Test — CLI scan command"]
    TASK_151["Feature Test — CLI upload command"]
    TASK_152["Feature Test — CLI upload-config command"]
    TASK_153["Feature Test — App home screen & BLE connection"]
    TASK_154["Feature Test — App profile list & profile editor"]
    TASK_155["Feature Test — App action editor (all action types)"]
    TASK_156["Feature Test — App upload screen & JSON preview"]
    TASK_157["Feature Test — E2E musician workflows & edge cases"]
    TASK_227["Defect — CLI scan broken on bleak ≥ 3.0 and on disabled BLE adapter"]
    TASK_228["Defect — BLE advertisement override leaks from test build into production, breaks HID discoverability"]
    TASK_229["Defect — BLE pairing policy (MITM=true) prevents Linux/Windows pairing of a no-display pedal"]
    TASK_230["Fix pre-commit hook — invokes deleted scripts/update_future_ideas.py"]
    TASK_231["Defect — Power LED hardwired to VCC; firmware error signals are invisible"]
    TASK_232["Defect — Action getJsonProperties placeholders lose information on round-trip"]
    TASK_233["Defect — CLI upload and firmware both accept schema-invalid profiles.json"]
    TASK_234["Defect — CLI upload shows raw Python traceback when BLE disconnects mid-transfer"]
    TASK_235["Defect — Hardware-identity BLE characteristic returned 4 bytes of pointer address instead of the string"]
    TASK_238["On-device verification — BLE config integration test on production firmware + pairing PIN smoke test"]
    TASK_240["Defect — firmware JSON parser undersized vs. advertised MAX_CONFIG_BYTES"]
    TASK_246["Defect — pairing_pin advertises PIN protection but pairs Just-Works (MITM bit never set)"]
    TASK_247["Automate BLE pairing smoke test via bluetoothctl (Linux)"]
    TASK_250["Defect — Android manifest missing BLE permissions; app cannot scan or connect"]
    TASK_251["Defect — system BACK exits the app from any sub-screen instead of popping"]
    TASK_252["Defect — Action Editor: Save doesn't navigate; Key (named) emits SendCharAction"]
    TASK_253["Defect — UI overflow in landscape & Media Key dropdown; Media Key filter not implemented"]
    TASK_255["Defect — Profile List blank-name validation invisible & Export UX (share sheet + UUID filename)"]
    TASK_256["Defect — Profile List accessibility (empty content-desc on FAB and row icons)"]
    TASK_257["Defect — Action Editor Key (raw HID) values like 0x28 are not parsed by firmware"]
    TASK_258["Defect — In-app BLE scan returns no devices while Android system Bluetooth picker sees the pedal"]
    TASK_261["Defect — Upload chunk size (510 B) exceeds Android writeWithoutResponse MTU cap (252 B)"]
    TASK_262["Defect — BLE scanner shows indefinite spinner when Bluetooth is off or permission is denied"]
    TASK_263["Defect — Scanner list truncates pedal device name to AwesomeStudioPe"]
    TASK_264["Defect — Profile List validation banner stays green when an action's value is schema-valid but runtime-unresolvable"]
    TASK_265["Defect — Action Editor crashes with Flutter dropdown assertion when opened on a profile whose action value cannot be resolved"]
    TASK_266["Defect — Upload Profiles swallows non-FlutterBluePlusException errors; UI hangs at chunk N/N with no SnackBar or dialog"]
    TASK_267["Defect — Upload Hardware Config button is unreachable; the app has no UI to load a hardware config from disk"]
    TASK_273["Defect — Firmware does not reboot after CONFIG_WRITE_HW upload"]
    TASK_276["Defect — Action Editor drops `longPress` field when an action is re-saved"]
    TASK_277["Defect — Key (raw HID) value field hint reads `e.g. 0x28` but `0x28` does not type Enter"]
    TASK_278["Usability session — Action Editor with non-developer tester (AE-U1, AE-U2)"]
    TASK_279["Defect — adding 8th profile silently rejected (no validation banner)"]
```

## iphone-app

_⚪ 0 open · 🔵 0 active · 🟡 2 paused · 🟢 0 closed · ░░░░░░░░░░ 0%_

```mermaid
kanban
  Open
  Active
  Paused
    TASK_158["Feature Test — Build, deploy and test the iOS app on iPhone"]
    TASK_161["Publish app to Apple App Store"]
  Closed
```

## video-content

_⚪ 7 open · 🔵 0 active · 🟡 0 paused · 🟢 0 closed · ░░░░░░░░░░ 0%_

```mermaid
kanban
  Open
    TASK_033["Create Setup/Installation Demo Video"]
    TASK_034["Create Button Configuration Demo Video"]
    TASK_035["Create Builder Workflow Demo Video"]
    TASK_036["Create Advanced Features Demo Video"]
    TASK_037["Create Real-World Usage Demo Video"]
    TASK_038["Create Troubleshooting Demo Video"]
    TASK_049["Setup video platform channel"]
  Active
  Paused
  Closed
```

## Other

_⚪ 3 open · 🔵 0 active · 🟡 0 paused · 🟢 12 closed · ████████░░ 80%_

```mermaid
kanban
  Open
    TASK_148["Reorganise Developer Documentation"]
    TASK_259["Android app test protocol — record device and Android version per test run"]
    TASK_260["Unify version numbers across all deliverables (firmware, app, CLI, simulator, …)"]
  Active
  Paused
  Closed
    TASK_140["Mobile-Responsive Layout for Simulator and Configurator Tools"]
    TASK_141["Auto-Install Python Packages in Dev Container"]
    TASK_142["Pre-Commit Hook for Dev Container Validation"]
    TASK_143["Pre-Commit Hook for Secrets Detection"]
    TASK_147["Android Emulator Setup Guide for App Development"]
    TASK_165["Flutter/Dart code quality pipeline — pre-commit hooks, CI, skills, and Makefile targets"]
    TASK_184["Schema defect — action value/pin fields not required"]
    TASK_186["Fix stale libdeps cache for local hardware libs in test environments"]
    TASK_236["Retire BLE_CONFIG_TEST_BUILD — update runner.py to test production firmware path"]
    TASK_237["BLE pairing PIN — configurable in hardware config, applied to NimBLE security"]
    TASK_239["Update wiring diagrams — add external pull-up resistors to button pins"]
    TASK_254["Enforce active status for paused tasks blocked by defects"]
```

